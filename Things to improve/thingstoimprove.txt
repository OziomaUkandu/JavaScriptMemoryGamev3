for (var i = 0; i < 10; i++) {
    document.write("<td></td>");
    if (i % 4 == 3) {
        document.write("</tr><tr>");
    }
}
//  to check for next row and build a table for the images.

document.write outputs directly when the JavaScript is run, we could build the HTML and then add to an Element 
by selecting the element like document.querySelector('.elemenetClass').innerHTML =

//current images aligned by width and inline-block class